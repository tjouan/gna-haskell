name: repoxler, repominder, repominer
Gná
http://en.wikipedia.org/wiki/Gn%C3%A1_and_H%C3%B3fvarpnir

grit, rugged, minigit?

notify about
  repositories not in master branch
  repositories with updated remotes

repositories as arguments or config file

Dir["#{ENV['HOME']}/tmp/**/.git"]

`discover' action that find all repos
  get their path, origin and generate a config (to clone them all later)
